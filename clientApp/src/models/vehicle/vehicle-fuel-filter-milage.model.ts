import { VehicleCommonModel } from './vehicle-common.model';

export class VehicleFuelFilterMilageModel extends VehicleCommonModel {
    nextFuelFilterChangeMilage: number=0;
    actualFuelFilterChangeMilage: number=0;
}
